using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[System.Serializable] //tells unity that the script is to save a stream of bytes and stuff
public class Save
{
    public int currentLevel = 0;
    public int LevelCheckpoint = 0;
    //Colectable in a bool array or something
}